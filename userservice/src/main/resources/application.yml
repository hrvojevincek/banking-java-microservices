spring:
  application:
    name: userservice
  datasource:
    url: jdbc:postgresql://localhost:5432/banking-user-db
    username: hrvojevincek
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect